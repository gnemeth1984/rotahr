# Category request reply — G2 / Gartner Digital Markets

Send after the Software Advice listing is submitted. One email, both requests.
Leads with the absent criterion on purpose: the Hospitality LMS request was
refused over a claim that didn't hold, so the fastest way through is to be the
one who names the gap.

**Subject:** Rotahr — category request: Restaurant Management + Employee Scheduling

---

Hi [name],

Following up on Rotahr's categories. I'd like to request **Restaurant
Management** and **Employee Scheduling**.

Before anything else: **Rotahr has no point of sale.** It doesn't take payments
from guests, take orders at the table or send tickets to the kitchen. It runs the
back of house around whatever till the venue already uses. I'd rather say that up
front than have it found later.

The reason earlier requests were hard to verify is my fault, not yours — every
screen that demonstrates these capabilities sat behind a login. I've now put a
public page up with real screenshots from a live venue, no signup needed:

**https://rotahr.com/product-tour**

Against the Restaurant Management criteria:

| Criterion | In Rotahr | Where to see it |
|---|---|---|
| Employee management | Yes | Rota, clock-in, time off, payroll hours — `/product-tour#rota` |
| Inventory management | Yes | Stock levels, reorder points, wastage — `/product-tour#stock-items` |
| Order management | Yes — **supplier purchase orders** | Order lists, draft → sent → received — `/product-tour#supplier-orders` |
| Reservations | Yes | Table floor plan and bookings — `/product-tour#floor-plan` |
| Accounting | Yes | Expenses, supplier statement reconciliation, P&L, tax summary — `/product-tour#supplier-statements` |
| Point of sale | **No** | Not built |

That's five of six. I understand the threshold is four.

One point of precision so nothing is taken the wrong way: **order management
here means purchase orders to suppliers** — building an order list off live stock
levels, sending it, receiving against it, then reconciling the supplier's
statement. It is not customer or table-side ordering. If that reads as a
mismatch for your definition, tell me and I'll drop the claim rather than argue
it.

For **Employee Scheduling**: rota building with live labour cost as shifts move,
shift templates and copy-week, availability and time off, manager-routed swaps,
publish-to-phone with push notifications, clock in/out with break entitlement
tracked against working time rules, and rostered-versus-actual hours for
payroll. Screens at `/product-tour#rota`.

If it's useful, the demo is open — no signup, resets itself, so nothing you click
matters: https://rotahr.com/auth/signin (demo panel is on that page).

Happy to answer anything or get on a call.

Gabor Nemeth
Founder, Rotahr
rotahr.com

---

## Notes before sending

- Anchor links (`#rota`, `#stock-items`, ...) work — each tour section carries an
  id matching its screenshot filename without the extension.
- Do **not** add Billing & Invoicing to this email. Rotahr processes supplier
  invoices, not customer ones, and the distinction is too easy to lose in a
  reviewer's summary.
- Do **not** re-raise Hospitality LMS. Certification expiry is tracked; courses
  are not hosted, and that request was already refused.
